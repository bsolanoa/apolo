// Versión visible en el topbar (al lado del logo). Se actualiza a mano en
// cada cambio de frontend que pida el usuario — no se genera automático
// desde package.json ni git. Convención: patch (+0.0.1) para ajustes/fixes
// chicos, minor (+0.1.0, patch a 0) para features nuevas, major para
// rediseños grandes.
export const APP_VERSION = "1.0.0";
